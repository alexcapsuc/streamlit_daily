assets_list = "SELECT DISTINCT asset_id, asset_name FROM highlow.marketspulse.tfc_assets ORDER BY 1"
durations_list = "SELECT DISTINCT fixed_duration_value as duration FROM highlow.marketspulse.tfc_option_definition ORDER BY 1"
